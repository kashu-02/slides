{
  "name": "marp-slides",
  "version": "1.0.0",
  "private": true,
  "description": "Marp presentation slides with CI/CD deployment",
  "scripts": {
    "build": "node scripts/build-all.js",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@marp-team/marp-cli": "^3.4.0"
  }
}
